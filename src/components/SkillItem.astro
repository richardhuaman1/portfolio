---
import Chip from './ui/Chip.astro';
import Typography from './ui/Typography.astro';
import type { Technology } from '@/utilities/types';

interface Props {
  title: string;
  technologies: Technology[];
}

const { technologies, title } = Astro.props;
---

<div>
  <Typography as='p' size='2xl' class='text-gray-200 font-medium'>{title}&nbsp;:</Typography>
  <div class='mt-2 flex items-center justify-start gap-2 h-full flex-wrap'>
    {technologies.map((t) => <Chip color={t.color}>{t.name}</Chip>)}
  </div>
</div>
