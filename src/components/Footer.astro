---
import Container from '@/components/Container.astro';
import Typography from '@/components/ui/Typography.astro';
import { me } from '@/data/me';
import { EnvService } from '@/services/env.service';
import { LAYOUT_SIZES } from '@/utilities/constants';

const footerHeight = LAYOUT_SIZES.footer;
const githubUrl = EnvService.getContact().github || '/';
---

<footer>
  <Container
    class:list='flex items-center justify-center w-full'
    style={`height:${footerHeight}px;`}
  >
    <Typography as='div' size='lg' color='light' class='text-center py-3'>
      Made with 💙 by <a
        href={githubUrl}
        target='_blank'
        rel='noopener noreferrer'
        role='link'
        class='text-primary underline hover:text-primary-300 active:text-primary-300 transition-colors duration-200'
        >{me.name}</a
      > • Powered by <a
        href='https://vercel.com'
        target='_blank'
        rel='noopener noreferrer'
        role='link'
        class='text-primary underline hover:text-primary-300 active:text-primary-300 transition-colors duration-200'
        >Vercel</a
      >
    </Typography>
  </Container>
</footer>
