---
interface Props {
  class?: string;
}

const { class: className, ...restProps } = Astro.props;
---

<div
  {...restProps}
  class:list={`mx-auto md:max-w-3xl lg:max-w-screen-lg px-4 md:px-8 lg:px-12 ${className}`}
>
  <slot />
</div>
